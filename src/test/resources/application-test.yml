spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/acquaintance
    username: root
    password: password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true
      show_sql: false
      highlight_sql: true

  flyway:
    enabled: false
#    baseline-on-migrate: true
#    locations: db/migration # 로컬 or 개발환경 테스트 DML은 코드로 생성
#    clean-disabled: false