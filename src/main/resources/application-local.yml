spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/acquaintance
    username: root
    password: password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true
      show_sql: false
      highlight_sql: true